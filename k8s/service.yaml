apiVersion: v1
kind: Service
metadata:
  name: cloud-eco-api-service
spec:
  type: NodePort
  selector:
    app: cloud-eco-api
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30080